{% extends 'layout.html.twig' %}
{% set active = 'form' %}

{% block content %}
<div id="main" role="main" class="container">
        {% set alertTypeAvaillable = [ 'info', 'success', 'warning', 'error'] %}
        {% for alert in alertTypeAvaillable %}
            {% for message in app.session.getFlashBag.get(alert) %}
    <div class="alert alert-{{ alert }}" >
        <button class="close" data-dismiss="alert">×</button>
                    {{ message|trans }}
    </div>
            {% endfor %}
        {% endfor %}

    <div class="page-header">
        <h1>Cadastro de empreendimento.</h1>
    </div>

    <div class="alert alert-warning">
         <button class="close" data-dismiss="alert">×</button>
        <strong>Atenção:</strong> Adicione todo os dados que tiver, caso não saiba mais detalhes cadastre mesmo assim nós vamos lhe ajudar, e assim que estiver cadastrado enviaremos um email com maiores detalhes, obrigado.
    </div>
    
    <form action="/morador/dados-update" method="post" {{ form_enctype(form) }} class="form-horizontal" role="form">
        {{ form_widget(form) }}        
    </form>

</div>
{% endblock %}
